apply plugin: 'com.android.application'

android {
    compileSdkVersion 29
    buildToolsVersion "29.0.2"


    defaultConfig {
        applicationId "com.lxzh123.sdkshelldemo"
        minSdkVersion 16
        targetSdkVersion 29
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

lint {
    def proName = getProject().getName()
    println("=====================" + proName + " task lint start.=======================")
    println("=====================" + proName + " task lint end.=========================")
    doLast {
        println("=====================" + proName + " task lint.doLast start.=======================")
        println("=====================" + proName + " task lint.doLast end.=========================")
    }
}

build {
    def proName = getProject().getName()
    doLast {
        println("=====================" + proName + " task build.doLast start.=======================")
        println("=====================" + proName + " task build.doLast end.=========================")
    }
}

lint.dependsOn([':corestub:build'])
build.dependsOn([':corestub:build', ':libshell:build'])

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation 'androidx.appcompat:appcompat:1.1.0'
    implementation 'androidx.constraintlayout:constraintlayout:1.1.3'
    testImplementation 'junit:junit:4.12'
    androidTestImplementation 'androidx.test:runner:1.2.0'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.2.0'
    compileOnly files('geetestsdk\\corestub.aar')
    implementation project(':libshell')
}
